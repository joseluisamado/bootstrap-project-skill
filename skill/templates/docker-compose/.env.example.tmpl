# {{project_name}} environment variables
#
# Copy to .env and fill in. The `.env` file is gitignored.

# Version tag that matches what `make build-prod` produced.
VERSION=0.1.0

# Public HTTP port — what users hit in the browser.
# Front this with Caddy / Nginx / Traefik for TLS in production.
HTTP_PORT=8080

# {{TODO: project-specific env vars. Each var the api container reads from
# docker-compose.yml `environment:` should be listed here with a comment
# explaining what it controls.}}
