# Artifact manifest — <slug>

Copy this file to `<slug>.md` for each artifact, fill it in, and add a row to
[INDEX.md](INDEX.md).

| Field | Value |
|---|---|
| **Name / filename** | `<exact filename>` |
| **Slug** | `<slug>` |
| **Type** | tool / binary / firmware / dataset / document / image / other |
| **sha256** | `<shasum -a 256 output>` |
| **Size** | `<bytes / human>` |
| **Obtained** | `<YYYY-MM-DD>` |
| **Source** | `<URL / forum thread + post / person / how acquired>` |
| **Terms** | `<license/terms of THIS artifact — not the project license>` |
| **Committed?** | yes (private archive) / no (referenced only) |
| **Scan status** | `<not-scanned / clamav clean YYYY-MM-DD / sandboxed / etc.>` |
| **Trust** | untrusted until scanned & sandboxed / trusted-source / verified |

## What it is

<one paragraph: what this artifact is and why the investigation needs it.>

## Provenance notes

<chain of custody, corroboration, anything notable about how it was found or
whether its authenticity was verified.>

## Handling

<how to use it safely — disposable VM? offline only? Any gotchas.>
