## YYYY-MM-DD — short title
**Phase:** <e.g. Phase 2>{{#if use_gates}} / <Gate B if relevant>{{/if}}
**Who:** <user | claude | both>
**Did:** <what was actually done — commands, downloads, physical steps>
**Result:** <what happened, what was learned; include error strings and identifiers verbatim>
**Artifacts:** <files added/changed; link evidence/{{#if has_artifacts}} or artifacts/manifests/{{/if}}>
**Next:** <the immediate next action this implies>
